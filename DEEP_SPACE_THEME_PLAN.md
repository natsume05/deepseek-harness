# 深空航线（星际拓荒）视觉计划

版本：v1
状态：**执行中**。将 modern 轨重塑为"星际拓荒"（Outer Wilds 启发的深空风格），与经典版（原版白纸观感）形成一眼可辨的区分；风格选项随之更名 **经典 / 深空**。自由创作，大胆执行。

## 1. 设计叙事

经典 = 白纸；**深空 = 鲸鱼在星海中航行**（DeepSeek 鲸鱼即飞船/航行生物）。星际拓荒的美学核心：深邃、宁静、好奇、暖橙的太阳光穿过蓝紫星云。

- **深色基调（夜航）**：深蓝黑底 + 星云光晕 + 极淡星尘 + 暖橙太阳光。
- **浅色基调（日间星云）**：淡蓝白底 + 极淡星云 + 暖橙点缀（尊重既有明暗主题维度，两态都有星际元素）。
- **暖橙（太阳）作为能量语言**：运行中、发送、警示、hero 太阳、鲸鱼伴游光——与 DeepSeek 蓝（星海）形成"日光 vs 星海"对比。
- **复古仪表线索**：等宽数字、状态"仪表灯"发光、进度刻度感。

## 2. 星际元素与线索清单

| 元素 | 载体 | 形态 |
|---|---|---|
| 星云 | 页面背景 | 蓝紫 radial 光晕（浅色极淡 / 深色明显），CSS 多背景 |
| 太阳 | hero + 运行态 | 暖橙 radial 光晕（右下小面积）；运行中状态点/发送按钮暖橙 |
| 星尘 | 页面 | 极淡噪点/小点阵（CSS 多 radial-gradient 或 data-URI，无大图） |
| 鲸鱼 | hero | FishLogo 放大 + 缓慢漂移/游动动画（品牌叙事） |
| 仪表灯 | 状态点/计量 | 发光圆点、刻度化进度 |
| 文案线索 | hero 口号 | "探索未至之境 / Into the Unknown" 天然契合，强化渐变 + 副标题式布局 |

## 3. Token 层（ui-theme）

新增/调整（深色轨为主，浅色轨同构降强度）：

| Token | 深空深色 | 深空浅色 | 经典（冻结） |
|---|---|---|---|
| `--dsw-alias-bg-base` | 深蓝黑 `#0C1120` | 淡蓝 `#F2F5FC` | 原白/原灰不变 |
| `--dsw-alias-bg-layer-1/2/3` | 深蓝灰阶梯 | 白/淡蓝灰阶梯 | 原值 |
| 新增 `--dsw-nebula-glow`（星云） | 蓝紫 2 团 + 暖橙 1 团 radial | 极淡版本 | `none` |
| 新增 `--dsw-stardust`（星尘） | 极淡噪点 | 极淡 | `none` |
| 新增 `--dsw-specific-solar`（暖橙） | `#FF9F43` 系 | 同 | 不消费 |
| `--dsw-gradient-brand` | 提亮星云蓝紫 | 原渐变 | 原值 |
| 卡片/输入/侧栏 | 深蓝灰玻璃 | 半透明白玻璃 | 原实色 |

**隔离**：所有深空专属 token 在 `body[data-ds-visual-style="classic"]` 显式重置；经典轨零变化。

## 4. 组件落地

1. **背景**：body 叠星云光晕 + 星尘（纯 CSS，性能可控）。
2. **卡片**（AppFrame）：深空下深蓝灰浮起 + 更柔阴影；浅色下白卡 + 极淡星云透出。
3. **侧栏/输入卡/浮层**：玻璃半透明 + blur（仅静态小面积，滚动区不碰 backdrop-filter）。
4. **hero**：鲸鱼游动 + 星云光场 + 暖橙太阳 + 渐变口号。
5. **状态与按钮**：运行中/发送暖橙；选中竖条星云渐变；滚动条 hover 星云蓝。
6. **深色同步**：所有深空元素在 dark 主题下完整呈现。

## 5. 风险与不变量

- classic 冻结（专属 token 全重置）；可访问性（正文对比度、渐变仅大字号品牌时刻）；性能（无滚动区玻璃、动画只 transform/opacity、星尘无大图）；语义 token 纪律；`test:gui` 全绿（DOM 快照无颜色）。
- **命名变更**：视觉风格选项 label 从"现代/Modern"改为"深空/Deep Space"（id 保持 `modern` 兼容，仅文案变），经典 label 不变。

## 6. 实施步骤

| 步骤 | 内容 |
|---|---|
| **S1 背景与基调** | 深空 token（bg/星云/星尘/暖橙）、风格选项更名、body 背景层、玻璃卡片/侧栏/输入卡 |
| **S2 元素与强调** | hero 鲸鱼 + 太阳 + 星云光场、状态/发送暖橙、选中/滚动条渐变 |
| **S3 细节与验收** | 复古仪表细节（刻度/等宽）、浅色轨校准、对比度/reduced-motion 复核、test:gui + 推送 |

每步独立提交（ui-redesign 分支）+ 全部门禁。
